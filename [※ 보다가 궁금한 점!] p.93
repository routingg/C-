#include <stdio.h>

void main() {

	int a,b;

	100+100 = a;  /* '대입 연산자(=)의 오른쪽에는 상수(숫자), 변수, 계산값이 모두 올 수 있다'는 규칙이 있다. */
	200 = b; /* 결론적으로 대입 연산자(=)의 왼쪽에는 변수만 올 수 있고 오른쪽에는 상수, 변수, 계산값, 함수 등 무엇이든지 올 수 있다. */

	printf("a의 값 ==> %d \n", a);
	printf("b의 값 ==> %d \n", b);
}

/* ※ 여기서 궁금한 건!
교수님이 계산식이 많을때라고 했나 뭔가 정확히 기억나지는 않지만
100 = a; 요로케 숫자를 먼저 쓰고 하면 아 얘 뭔가 아는 아이구나,, 라는 느낌을 주는 코딩이 있다고 하셨는데 그게 뭐였을까?
내일 시험 전까지 강의 노트 보면서 꼭 알아내보기!!! */
