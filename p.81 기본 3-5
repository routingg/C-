#include <stdio.h>

void main() {

	printf("%d\n", 123);
	printf("%5d\n", 123);
	printf("%05d\n", 123);

	printf("%f\n", 123.45);
	printf("%7.1f\n", 123.45);
	printf("%7.3f\n", 123.45);

	printf("%s\n", "Basic-C");
	printf("%10s\n", "Basic-C");

}

/* <출력 결과>

123
  123 >> %5d는 다섯자리로 채워라~ 근데 오른쪽 정렬~~ 남은데는 공백으로 놔두고!
00123 >> %05d는 오른쪽 정렬로 다섯자리로 채우는거 인데 남은데 0으로 채우면 됨!
123.450000 >> %f는 실수 표현하는 건데 소수점 6자리까지 표현하나보다!
  123.5 >> %7.1f는 소수점을 포함한 전체 자리인 일곱자리를 확보한 후에 소수점 아래는 한 자리만 차지한다는 의미 = 소수점 아래 첫째 자리만 출력하고, 소수점 아래 둘째 자리에서 반올림 한다...
123.450 >> %7.3f는 소수점 아래 셋째 자리까지 출력하고 빈칸은 0으로 채운다.
Basic-C >> %s는 그냥 문자열 의미하는 듯? string
   Basic-C >> %10s는 우선 10자리 확보하고 오른쪽 정렬로 적어~~

*/
