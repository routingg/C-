#include <stdio.h>

void main() 
{
	int a, b;
	float c, d;

	a = 100;
	b = a;

	c = 111.1f; /* 음 뒤에 붙은 f는 실수 선언 방법(float & double) 중 float라는 의미, 컴퓨터가 double로 인식해서 컴파일할때 약한 경고가 나옴! 큰 문제는 아니지만 float형은 되도록 숫자 뒤에 f를 붙이는 게 좋다고 합니당 설명은 나중에 나오면 다시 적도록 하겠음! */
	d = c;

	printf("a, b의 값 ==> %d, %d \n", a, b);
	printf("c, d의 값 ==> %d, %d \n", c, d); /* 자꾸 실수형을 %d로 넣는 실수를 한다!! %f로 넣자잉!! */
}


/* 그래서 고치면...

#include <stdio.h>

void main() 
{
	int a, b;
	float c, d;

	a = 100;
	b = a;

	c = 111.1f;
	d = c;

	printf("a, b의 값 ==> %d, %d \n", a, b);
	printf("c, d의 값 ==> %5.1f, %5.1f \n", c, d); >> 여기서 %5.1은 5자리 확보하고 반올림해서 소수점 첫째자리까지 나타내라는 의미!	
}

요거 하면 결과는?

a, b의 값 ==> 100, 100
c, d의 값 ==> 111.1, 111.1

요러케 제대로 나온답니당!

*/
